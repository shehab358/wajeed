import 'package:flutter/material.dart';

class MyShopTab extends StatelessWidget {
  const MyShopTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('My Shop'),
    );
  }
}