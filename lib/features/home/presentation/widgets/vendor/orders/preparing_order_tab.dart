import 'package:flutter/material.dart';

class PreparingOrderTab extends StatelessWidget {
  const PreparingOrderTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Preparing Order'),
    );
  }
}
